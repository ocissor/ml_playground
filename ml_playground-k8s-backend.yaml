apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-playground-backend
  labels:
    app: ml-playground-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ml-playground-backend
  template:
    metadata:
      labels:
        app: ml-playground-backend
    spec:
      containers:
      - name: ml-playground-backend-container
        image: ml_playground_backend_image:latest  # Use local image
        imagePullPolicy: IfNotPresent
        ports:
          - containerPort: 8000
        envFrom:
          - secretRef:
              name: ml-playground-secrets 

---
apiVersion: v1
kind: Service
metadata:
  name: ml-playground-backend-service
spec:
  type: ClusterIP
  selector:
    app: ml-playground-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000